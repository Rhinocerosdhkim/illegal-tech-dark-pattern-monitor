"""Capture layer — browser, path, Evidence Dossier.

Owned by the capture side of the split (ARBEITSTEILUNG_Technik.md 2). It is
the layer that rots: markup changes, bot detection appears, selectors break.
Everything site-specific therefore lives in data/targets/*.yaml, never here.

See README.md in this folder for what is ported and what is still missing.
"""
