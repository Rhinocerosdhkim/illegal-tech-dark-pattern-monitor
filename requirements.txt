PyYAML>=6.0
Jinja2>=3.1
playwright>=1.40

# capture layer — only needed for `python -m dpm capture`.
# The evaluation layer runs without them.
playwright-stealth>=1.0
google-genai>=1.0

# Only for `python -m dpm ui`. Everything else -- capture,
# assess, report, overview, timeline, rebuild -- runs without them.
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
